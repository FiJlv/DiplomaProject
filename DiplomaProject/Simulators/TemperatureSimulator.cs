using DiplomaProject.Models;
using System.Collections.Generic;

namespace DiplomaProject.Simulators
{
    public static class TemperatureSimulator
    { 
        //public static List<Temperature> Generate()
        //{
        //    Random random = new Random();

        //    List<Temperature> values = new List<Temperature>(8);

        //    for(int i = 0; i < values.Count; i++) 
        //    {
        //        values[i].Value = random.Next(30,45);
        //    }

        //    return values;
        //}
    }
}
